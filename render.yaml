services:
  - type: web
    name: nbis-fingerprint-matcher
    env: docker  # CRITICAL: This tells Render to use Dockerfile
    region: oregon
    plan: starter
    dockerfilePath: ./Dockerfile  # Explicitly specify Dockerfile path
    dockerContext: .
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 10000